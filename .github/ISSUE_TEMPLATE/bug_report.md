---
name: Bug report
about: Something in the Monitor doesn't work as documented
labels: bug
---

**What happened**
A clear description of the incorrect behavior.

**What you expected**
What should have happened instead.

**Steps to reproduce**

1. ...
2. ...

**Environment**

- Monitor version (see the Operational Status page, or `git rev-parse HEAD`):
- Node.js version:
- Deployed how (Docker Compose / bare `node dist/index.js` / other):
- MongoDB configured? (yes / no — endpoints fall back to synthetic data without it)

**Logs / screenshots**
Paste any relevant `app.log` output or a screenshot. Redact anything from a real
LibreChat deployment (user emails, prompts, connection strings) before pasting.
